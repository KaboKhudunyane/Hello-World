// Importing the necessary module from the 'react' library
import {} from "react";

// Functional component named 'App'
const App = () => {
  // Returning JSX (React elements) representing the component's UI
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

// Exporting the 'App' component as the default export of this module
export default App;
